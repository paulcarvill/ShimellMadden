<section class="blog projectitem">
  <header>
    <%= render "shared/nav" %>
  </header>
  <aside>
    <h2><%= @project.name %></h2>
    <p class="desc"><%= @project.description %></p>
    <p><%= link_to 'Back to projects', projects_path %></p>
  </aside>
  <article>
    <% @project.items.each do |item|%>
      <%= image_tag item.itemsImage.url %>
    <% end %>
  </article>
 </section>

 <%= render "shared/footer" %>
